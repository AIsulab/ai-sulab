# 데일리 핫이슈 블로그 자동화 프로젝트 - 인수인계 문서

**작성일**: 2026-05-18
**이전 AI**: Claude (Anthropic) - 세션 한도 도달로 인계
**현재 단계**: Phase 1.5 진행 중 (약 80% 완료)

---

## 🎯 프로젝트 개요

### 최종 목표
대한민국 정부지원금/정책 정보 단일 니치 블로그를 자동으로 운영하여 **Google AdSense 승인 후 광고 수익을 창출**하는 것.

### 핵심 사양
- **블로그**: https://mytempick.blogspot.com (제목: "데일리 핫이슈")
- **GitHub 저장소**: github.com/Alsulab/ai-sulab
- **로컬 작업 경로**: `C:\website`
- **현재 브랜치**: `autopost-refactor-niche-mode`
- **기술 스택**:
  - Node.js 20+ (cheerio가 undici → File global 요구로 필수)
  - Gemini 2.5 Flash (`@google/genai` SDK)
  - Google Blogger API (OAuth 인증)
  - Pollinations.ai (이미지 생성, 무료 API)
  - GitHub Actions (스케줄링 + 수동 트리거)
  - cheerio (HTML 파싱)

### 발행 스케줄
- 하루 2회 발행 (오전·오후 슬롯) - KST 기준
- 오전: 5~14시, 오후: 그 외
- 슬롯별 프로그램 선택 공식: `(dayOfYear * 2 + slotIndex) % programs.length`

---

## ✅ 완료된 작업

### Phase 1: 단일 니치 + 출처 검증 (완료)
- ✅ 트렌드 모드(`getTrendingKeyword`) 완전 제거
- ✅ 정부지원금/정책 정보 모드로 단일화
- ✅ `verified_programs.json` 생성 - 24개 프로그램, 10개 카테고리 균형 배치
- ✅ `fetchSourceContent()` 함수 추가 (cheerio 기반)
- ✅ `generateContent()` 사실 기반 엄격 프롬프트
- ✅ Pollinations.ai 업그레이드 (model=flux, 1200x630, enhance=true)
- ✅ 이미지 프롬프트 "구체적·실사적" 방향으로 재작성
- ✅ 워크플로우 Node.js 18 → 20 업그레이드
- ✅ GitHub Actions checkout/setup-node v3 → v4 업그레이드
- ✅ package.json: cheerio 추가, rss-parser 제거

### Phase 1.5: Google Search Grounding + 가독성 강화 (진행 중)
- ✅ `verified_programs.json` 보강 완료
  - 24개 모든 프로그램에 `coreSummary`, `officialSiteName`, `lastVerified` 필드 추가
  - 검증 명령으로 누락 없음 확인됨
- ✅ `auto_post.js`에 Google Search Grounding 도입 코드 작성 완료
  - `@google/genai` SDK의 `tools: [{ googleSearch: {} }]` 사용
  - 공식 문서(ai.google.dev/gemini-api/docs/google-search) 참조하여 구현
- ✅ HTML 스타일 전면 개선 (전 연령대 가독성, 반응형)
- ✅ Syntax check 통과 (모듈 로드 성공)
- ⏳ **여기서 인계: 작업 4, 5, 6 남음**

---

## 🚧 즉시 이어서 해야 할 작업

### 작업 4: 콘텐츠 품질 미달 시 발행 차단 안전장치 추가

`publishToBlogger` 함수에 다음 검증 추가:

```javascript
// 콘텐츠 품질 검증
const qualityFlags = [
  "확인할 수 없습니다",
  "명시되어 있지 않습니다",
  "참고 자료에",
  "제공된 정보로"
];
const flagCount = qualityFlags.reduce((count, flag) => {
  const matches = newContent.htmlBody.match(new RegExp(flag, 'g'));
  return count + (matches ? matches.length : 0);
}, 0);

if (flagCount >= 3) {
  console.error(`⚠️ 콘텐츠 품질 미달 (검증 부족 표현 ${flagCount}회 등장). 발행 스킵.`);
  return; // 발행하지 않고 정상 종료
}
```

목적: Google Search Grounding이 실패하거나 결과가 빈약할 경우, 또 빈 액자 글이 발행되는 사고를 방지.

### 작업 5: commit / push

```bash
git add auto_post.js verified_programs.json package.json
git commit -m "feat(phase1.5): google search grounding + readable styling for all ages"
git push origin autopost-refactor-niche-mode
```

또는 main 브랜치로 머지 후 푸시. 사용자에게 어느 쪽 원하는지 확인.

### 작업 6: GitHub Actions 수동 실행 및 검증

1. `gh workflow run auto_post.yml` 또는 GitHub 웹 UI Actions 탭에서 수동 실행
2. 실행 완료 후 발행된 블로그 글 URL 확인
3. **다음 항목을 사용자와 함께 검증**:
   - ✅ 제목에 정확한 연도(2026) 사용됐는지
   - ✅ 본문에 "확인 필요" 류 표현이 사라졌는지 (3회 이상이면 안전장치로 발행 차단됨)
   - ✅ 구체적 정보(금액, 자격, 기한)가 본문에 포함됐는지
   - ✅ 이미지가 본문과 시각적으로 연결되는지
   - ✅ 이미지 크기가 max-width 480px로 작게 표시되는지
   - ✅ 폰트 크기 18px, 행간 1.85 적용됐는지 (가독성)
   - ✅ 모바일에서 반응형 작동하는지

---

## 📋 사용자 결정 사항 (절대 변경 금지)

이전 대화에서 사용자가 명확히 결정한 내용:

1. **이미지 크기 전략**: 한국 정부지원금 상위 노출 블로그 패턴 따름
   - 생성: 1200x630 고품질 (Pollinations.ai)
   - 본문 표시: max-width 480px (작게)
   - SNS 공유용 OG 이미지로는 풀 사이즈 활용

2. **타깃 독자**: 전 연령대 (부모님 세대 포함)
   - 폰트 18px (모바일 17px)
   - 행간 1.85
   - 단락 간격 1.5em
   - 글자색 #2c2c2c (순검정 X)

3. **모바일/데스크탑**: 둘 다 균형있게
   - 768px 미디어 쿼리로 반응형
   - 본문 폭 max 720px

---

## ⚠️ 알려진 이슈 (이전 발행 사례 분석)

### 첫 발행 글 (실패 사례)
- **URL**: https://mytempick.blogspot.com/2026/05/blog-post_18.html
- **문제**: "공식 사이트에서 확인 필요" 표현 11번 반복 → 빈 액자 글
- **원인**: `fetchSourceContent`가 정부 사이트 JS 렌더링 콘텐츠를 가져오지 못함
- **해결**: Google Search Grounding 도입 (Phase 1.5에서 처리 중)
- **조치**: 이 글은 Blogger 관리 페이지(blogger.com)에서 **수동 삭제 권장** - SEO 페널티 방지

### 추가 발견 문제
- Gemini가 학습 데이터 기준으로 "2024년"이라 표기 (실제는 2026년)
- 해결: Phase 1.5 프롬프트에 `현재 연도: 2026년`, `작성 일자: ${new Date().toISOString().slice(0,10)}` 명시적 주입

---

## 🛠️ 환경 변수 (.env)

로컬 `.env` 파일과 GitHub Actions Secrets에 동일하게 설정되어 있음:

```
GEMINI_API_KEY=...
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
GOOGLE_REFRESH_TOKEN=...
BLOGGER_BLOG_ID=...
```

⚠️ 새 AI는 절대 .env 파일을 수정하거나 노출하지 말 것.

---

## 📂 주요 파일 구조

```
C:\website\
├── auto_post.js              # 메인 자동 포스팅 스크립트
├── verified_programs.json    # 24개 정부지원금 프로그램 DB
├── package.json              # dependencies
├── .github/workflows/
│   └── auto_post.yml         # GitHub Actions 워크플로우 (Node 20)
├── scratch_json_test.js      # 테스트 파일 (운영 무관, 무시)
├── task_phase15.md           # Phase 1.5 작업 지시문 (참고용)
└── .env                      # 환경 변수 (절대 수정 금지)
```

`task_phase15.md`에 Phase 1.5 전체 명세가 들어있습니다. **새 AI는 가장 먼저 이 파일을 읽으세요.**

---

## 🔜 향후 로드맵 (Phase 1.5 이후)

### Phase 2: 콘텐츠 깊이 강화
- 글 분량 3000~5000자로 확대
- JSON-LD 스키마 마크업 자동 삽입 (FAQ, HowTo, Article)
- 메타 디스크립션, Open Graph 태그
- 내부 링크 자동 생성 (이전 글 중 관련 글 3개)
- About/Contact/Privacy 페이지 생성 (AdSense 필수 요건)

### Phase 3: 기술적 SEO
- 자체 도메인 이전 (Blogspot → .co.kr 등) - AdSense RPM 30~50% 상승 효과
- Search Console 등록
- 사이트맵 자동 제출
- 봇/크롤러 친화적 robots.txt

### Phase 4: AdSense 신청
- 30~50편 양질 콘텐츠 누적 후 신청 (구글 가이드라인 준수)
- 초기 트래픽 확보 (소셜 공유, 직접 방문 유도)
- 신청 전 모든 페이지 수동 검토

---

## 💡 다음 AI에게 권장사항

1. **task_phase15.md 파일을 가장 먼저 읽으세요** - Phase 1.5 전체 명세가 있음
2. **작업 4, 5, 6만 마저 처리하면 Phase 1.5 완료** - 새로 만들 게 거의 없음
3. **이전 Claude의 진단을 신뢰하고 같은 방향 유지** - 사용자가 이 방향에 명시적으로 동의함
4. **모르는 영역에서는 추측 금지**:
   - Gemini API의 Google Search Grounding 활용 방식은 공식 문서 직접 확인:
     https://ai.google.dev/gemini-api/docs/google-search
   - 정부 프로그램 정보는 임의로 추정하지 말 것
5. **GitHub Actions 수동 실행 후 결과는 사용자 검토 거칠 것**
   - 빈 액자 글이 또 나오는지가 핵심 검증 포인트
   - 안전장치(작업 4)가 작동했는지 로그로 확인
6. **권한 프롬프트 처리**:
   - `*.go.kr`, `*.gov.kr`, `ai.google.dev`, `github.com` 등은 자동 승인 OK
   - 처음 보는 일반 도메인은 1회 승인만 (don't ask again 금지)
   - `node *`, `git commit -m *` 같은 광범위 허용은 권장하지 않음

---

## 📞 사용자 컨텍스트

- **언어**: 한국어 (위치: Seoul)
- **기술 수준**: 중간 (GitHub Actions, VS Code, PowerShell 다룰 수 있음)
- **의사결정 스타일**: 명확하고 빠름, 솔직한 분석 선호
- **선호 진행 방식**: 단계적 진행, 각 단계 결과 검토 후 다음 진행
- **자주 사용하는 도구**: VS Code 통합 터미널에서 Claude Code 또는 Google Antigravity 실행

⚠️ **중요**: 작업이 끝날 때마다 결과(스크린샷, 로그, URL)를 사용자에게 보여주고 다음 단계 동의를 받은 후 진행할 것. 단번에 모든 걸 자동 진행하지 말 것.

---

## 🆘 트러블슈팅 (이전에 겪었던 문제들)

| 증상 | 원인 | 해결 |
|------|------|------|
| `Cannot use import statement outside a module` | package.json에 `"type": "module"` 누락 | 추가 |
| `ReferenceError: File is not defined` | Node.js 18에서 cheerio→undici 호환 안 됨 | Node 20 업그레이드 |
| `401 Invalid authentication credentials` (Claude Code) | API 키 만료/오류 | `/login` 명령 또는 API 키 재발급 |
| PowerShell 긴 텍스트 붙여넣기 잘림 | 터미널 한계 | 파일로 저장 후 "이 파일 읽고 실행" 방식 |
| 권한 프롬프트로 작업 중단 | acceptEdits 모드는 file edit만 자동 | `claude --dangerously-skip-permissions` 또는 도메인별 don't ask again |

---

## 📝 마지막 액션 (인계 직전 상태)

이전 Claude 세션에서 마지막으로 실행하려던 명령:

```
node -e "import('./auto_post.js').catch(e => { 
  if(e.message.includes('env') || e.message.includes('API') || e.message.includes('GEMINI')) { 
    console.log('✅ 모듈 로드 성공 (환경변수 없어 API 실패는 정상)'); 
  } else { 
    console.error('❌ 모듈 에러:', e.message); 
    process.exit(1); 
  } 
})" 2>&1 | head -5
```

이 syntax check는 통과한 상태로 추정됩니다 (90% 진행률 시점).

**다음 AI는**:
1. 이 syntax check 결과를 사용자에게 확인 받고
2. 작업 4 (안전장치) 진행
3. 작업 5 (commit/push) 진행
4. 작업 6 (GitHub Actions 수동 실행 + 결과 검증) 진행
5. 결과를 사용자가 확인 후 Phase 2 진행 여부 결정

---

**행운을 빕니다. 사용자분은 좋은 의사결정자이고 협업하기 편한 분입니다. 🚀**
